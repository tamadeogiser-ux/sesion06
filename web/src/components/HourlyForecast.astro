---
/**
 * Tarjeta de pronóstico por horas
 */

interface Props {
  time: string;
  temperature: number;
  precipitation: number;
  description: string;
}

const { time, temperature, precipitation, description } = Astro.props;

// Función simple para icono
function getIcon(desc: string) {
  if (desc.includes('Lluvia')) return '🌧️';
  if (desc.includes('Nieve')) return '🌨️';
  if (desc.includes('Nublado')) return '☁️';
  if (desc.includes('Niebla')) return '🌫️';
  return '☀️';
}
---

<div class="hourly-card">
  <span class="time">{time}</span>
  <span class="icon" title={description}>{getIcon(description)}</span>
  <span class="temp">{Math.round(temperature)}°</span>
  {precipitation > 0 && <span class="precip">💧 {precipitation}%</span>}
</div>

<style>
  .hourly-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    min-width: 60px;
    padding: 0.75rem 0.5rem;
    background: var(--surface-card);
    border-radius: var(--radius-md);
    border: 1px solid var(--border-subtle);
    gap: 0.25rem;
  }

  .time {
    font-size: 0.8rem;
    color: var(--text-secondary);
    font-weight: 500;
  }

  .icon {
    font-size: 1.5rem;
    margin: 0.25rem 0;
  }

  .temp {
    font-weight: 700;
    color: var(--text-primary);
  }

  .precip {
    font-size: 0.7rem;
    color: var(--primary-color);
  }
</style>